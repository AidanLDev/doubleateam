import { Divider } from '@chakra-ui/react';

export default function StyledDivider() {
  return (
    <Divider
      style={{
        margin: '12px 0',
        borderColor: 'gray'
      }}
    />
  );
}
